import * as React from 'react';

export const Onboarding = () => {
  return (
    <div>
      <p>Select both a curve and any object to have it repeat along the curve.</p>
      <p>If text is selected, the text will follow the curve instead.</p>
    </div>
  );
};
